{
  "name": "url-es3",
  "description": "The core `url` packaged standalone for use with Browserify. (with ES3 support)",
  "version": "0.10.3",
  "dependencies": {
    "indexof": "0.0.1",
    "object-keys": "^1.0.3",
    "punycode": "1.3.2",
    "querystring-es3": "^0.2.1"
  },
  "main": "./url.js",
  "devDependencies": {
    "assert": "^1.3.0",
    "foreach": "^2.0.5",
    "mocha": "^2.2.1",
    "zuul": "^2.1.1"
  },
  "scripts": {
    "test": "mocha --ui qunit test.js && zuul -- test.js",
    "test-local": "zuul --local -- test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jasonkarns/node-url.git"
  },
  "license": "MIT"
}
